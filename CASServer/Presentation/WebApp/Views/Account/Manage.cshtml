@model  CASServer.Models.LocalPasswordModel
@{
    this.ViewBag.Title = "管理帐户";
}

<hgroup class="title">
    <h1>@this.ViewBag.Title.</h1>
</hgroup>

<p class="message-success">@this.ViewBag.StatusMessage</p>

<p>你已使用 <strong>@this.User.Identity.Name</strong>. 身份登录。</p>

@if (this.ViewBag.HasLocalPassword)
{
    @this.Html.Partial("_ChangePasswordPartial")
}
else
{
    @this.Html.Partial("_SetPasswordPartial")
}

<section id="externalLogins">
    @this.Html.Action("RemoveExternalLogins")

    <h3>添加外部登录</h3>
    @this.Html.Action("ExternalLoginsList", new {ReturnUrl = this.ViewBag.ReturnUrl})
</section>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}